{% extends '_parts/dashboard.part.html.twig' %}

{% block title %}Add Team{% endblock %}

{% block dashboardBody %}
<div class="body-wrapper">
        <div class="container-fluid">
          <div class="card bg-light-info shadow-none position-relative overflow-hidden">
            <div class="card-body px-4 py-3">
              <div class="row align-items-center">
                <div class="col-9">
                  <h4 class="fw-semibold mb-8">Add Team</h4>
                  <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                      <li class="breadcrumb-item"><a class="text-muted" href="{{ path('app_dashboard') }}">Dashboard</a></li>
                      <li class="breadcrumb-item"><a class="text-muted" href="{{ path('app_team') }}">Team</a></li>
                      <li class="breadcrumb-item" aria-current="page">Add</li>
                    </ol>
                  </nav>
                </div>
                <div class="col-3">
                  <div class="text-center mb-n5">  
                    <img src="{{ asset('admin/images/breadcrumb/ChatBc.png') }}" alt="" class="img-fluid mb-n4">
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="card">
            <div class="card-body">
            {{ form_start(teamForm) }}
              <div class="row">
              
                    <div class="col-lg-6 d-flex align-items-stretch">
                      <div class="card w-100 position-relative overflow-hidden">
                        <div class="card-body p-4">
                          <h5 class="card-title fw-semibold">Add team</h5>
                          <p class="card-subtitle mb-4">Add the new team here</p>
                          {{ form_row(teamForm.name) }}
                          {{ form_row(teamForm.description) }}
                          {{ form_row(teamForm.country) }}
                          {{ form_label(teamForm.balance) }}
                          <div class="input-group mb-3">
                          <span class="input-group-text">{{ siteOptions.siteCurrency }}</span>
                          {{ form_widget(teamForm.balance) }}
                          {{ form_errors(teamForm.balance) }}
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="col-lg-6 d-flex align-items-stretch">
                      <div class="card w-100 position-relative overflow-hidden">
                        <div class="card-body p-4">
                        
                          <h5 class="card-title fw-semibold">Team logo</h5>
                          <p class="card-subtitle mb-4">Change the team logo from here</p>
                          <div class="text-center picture-upload-container">
                          {{ form_row(teamForm.logoFile) }}
                            <img src="{{ asset('admin/images/logos/team.png') }}" alt="" class="img-fluid" width="120">
                            <div class="d-flex align-items-center justify-content-center my-4 gap-3">
                              {{ adminUploadTriggerButton('Choose') }}
                            </div>
                            <p class="mb-0">Allowed JPG or PNG. Max size of 1Mb</p>
                          </div>
                          
                        </div>
                      </div>
                    </div>
                    <div class="col-lg-3">
                    {{ adminSubmitButton('Add', '', {} , 'btn btn-primary w-auto') }}
                    </div>
                    
                  </div>
                  {{ form_end(teamForm) }}
            </div>
          </div>
        </div> 
      </div>
{% endblock %}

{% block additionalScripts %}
<script src="{{ asset('admin/libs/select2/dist/js/select2.full.min.js') }}"></script>
{% endblock %}
{% block additionalStyles %}
<link rel="stylesheet"  href=" {{ asset('admin/libs/select2/dist/css/select2.min.css') }}">
{% endblock %}